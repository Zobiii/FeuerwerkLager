@page
@model FeuerwerkLager.Pages.Locations.DeleteModel
@{
    ViewData["Title"] = "Lagerplatz löschen";
}

<div class="page-header">
    <h2>Lagerplatz löschen</h2>
    <p>Nur leere Lagerplätze können gelöscht werden.</p>
</div>

<div class="form-card">
    @if (!Model.Locations.Any())
    {
        <p class="text-muted">
            Es sind aktuell keine leeren Lagerplätze vorhanden, die gelöscht werden könnten.
        </p>
        <a asp-page="/Locations/Index" class="btn-secondary-custom">
            ⬅️ Zurück zur Lagerplatzliste
        </a>
    }
    else
    {
        <form method="post">
            <div asp-validation-summary="All" class="validation-summary-errors mb-3"></div>

            <div class="form-grid">
                <div class="form-group">
                    <label for="locationSelect">Lagerplatz auswählen</label>
                    <select asp-for="SelectedLocationId" class="form-select" id="locationSelect">
                        <option value="">-- Lagerplatz wählen --</option>
                        @foreach (var l in Model.Locations)
                        {
                            <option value="@l.Id">@l.Name</option>
                        }
                    </select>
                </div>
            </div>

            <p class="mt-2 text-muted" style="font-size:0.85rem;">
                Achtung: Der ausgewählte Lagerplatz wird endgültig gelöscht. Diese Aktion kann nicht rückgängig gemacht werden.
            </p>

            <div class="mt-3 d-flex gap-2">
                <button type="submit" class="btn-danger-custom">
                    🗑️ Lagerplatz löschen
                </button>
                <a asp-page="/Locations/Index" class="btn-secondary-custom">
                    ⬅️ Abbrechen
                </a>
            </div>
        </form>
    }
</div>
