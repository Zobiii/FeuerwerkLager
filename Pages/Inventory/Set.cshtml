@page
@model FeuerwerkLager.Pages.Inventory.SetModel
@{
    ViewData["Title"] = "Inventur";
}

<div class="page-header">
    <h2>Inventur - Bestand direkt setzen</h2>
    <p>Gez\u00e4hlte Menge f\u00fcr einen Artikel an einem Lagerplatz als neuen Soll-Bestand eintragen.</p>
</div>

<div class="form-card">
    <form method="post">
        <div asp-validation-summary="All" class="validation-summary-errors mb-3"></div>

        <div class="form-grid two-columns">
            <div class="form-group">
                <label for="articleSelect">Artikel</label>
                <select asp-for="SelectedArticleId" class="form-select" id="articleSelect">
                    @foreach (var a in Model.Articles)
                    {
                        <option value="@a.Id">@a.Name (@a.Company)</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label asp-for="NewFullUnits">Volle Einheiten (Soll)</label>
                <input asp-for="NewFullUnits" class="form-control" type="number" min="0" />
            </div>

            <div class="form-group">
                <label asp-for="NewLoosePieces">Lose Einzelteile (Soll)</label>
                <input asp-for="NewLoosePieces" class="form-control" type="number" min="0" />
            </div>

            <div class="form-group">
                <label>Lagerplatz</label>
                <select asp-for="SelectedLocationId" class="form-select">
                    <option value="">frei (nicht zugeordnet)</option>
                    @foreach (var l in Model.Locations)
                    {
                        <option value="@l.Id">@l.Name</option>
                    }
                </select>
            </div>
        </div>

        <p class="mt-2 text-muted" style="font-size:0.85rem;">
            Hinweis: Lose Einzelteile m\u00fcssen kleiner als eine volle Einheit sein (St\u00fccke pro Artikel beachten).
            Bei <strong>0</strong> wird die Position entfernt.
        </p>

        <div class="mt-3 d-flex gap-2">
            <button type="submit" class="btn-main">
                ?? Inventur \u00fcbernehmen
            </button>
            <a asp-page="/Index" class="btn-secondary-custom">
                ?? Zur\u00fcck zum Dashboard
            </a>
        </div>
    </form>
</div>
